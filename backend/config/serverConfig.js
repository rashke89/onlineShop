const port = 4000;
const ServerConfig = {
    port: port,
    serverRunningMsg: `Server is running on port: ${port}`,
    serverLink: `Server is running on: http://localhost:${port}`,
};

module.exports = ServerConfig;
